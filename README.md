# go-matrix-multiplication
The same assignment I did for csx55 but this time in Go and with the use of goroutines.

By default, does matrix multiplication with a 3000x3000 matrix. You can change this by editing the argument in `run.sh`.

```
chmod +x run.sh
./run.sh
```
